<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
       
        <title>{% block title %}Chiffa{% endblock %}</title>
        {% block stylesheets %}
         {% stylesheets '@JbnetBootstrapThemeBundle/Resources/public/css/bootstrap.min.css' %}
          <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}
        {% stylesheets '@JbnetBootstrapThemeBundle/Resources/public/css/style.css' %}
          <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}
        {% endblock %}
        {#<link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />#}   
        <link rel="icon" type="image/png" href="{{asset('bundles/jbnetbootstraptheme/images/favicon.png') }}" />#
    </head>   
       <!-- banner -->
<div class="navigation">
                 <div class="logo">
				<a href="index.html"><img src="{{asset('bundles/jbnetbootstraptheme/images/logo2.png') }}" alt="Chifaa"></a>
	    	 </div>

	    	 <div class="menu">
	    	 	<div class="dropdown">
				  <a href="#">PRÉSENTATION</a>
				  <div class="dropdown-content">
				      <a href="#">Principe</a>
				      <a href="#">Valeurs</a>
				      <a href="#">Nos actions</a>
				      <a href="#">Qui sommes-nous ?</a>
				  </div>
				</div>

				<div class="dropdown">
					<a href="#">AIDEZ-NOUS</a>
					<div class="dropdown-content">
						<a href="#">Prévoir une visite</a>
						<a href="#">Devenir bénévole</a>
				    </div>
				</div>
				 <a href="{{ path('Contact') }}">CONTACT</a>
				<a href="#">PARTENAIRES</a>
	    	 </div>	

	<div class="banner" id="home">
		<div class="banner-content">
			<h1>Chifaa</h1>
			<h2>Association Humanitaire</h2>
		</div>
	</div>
</div>
<a href="#" id="toTop" style="display: inline;"><span id="toTopHover"></span>To Top</a> <!-- Aller vers le haut -->
        {% block body %}{% endblock %}
        {% block javascripts %}
        {% javascripts '@JbnetBootstrapThemeBundle/Resources/public/js/jquery-3.2.1.min.js' %}
                <script src="{{ asset_url }}"></script>
        {% endjavascripts %}
        {% javascripts '@JbnetBootstrapThemeBundle/Resources/public/js/bootstrap.min.js' %}
                <script src="{{ asset_url }}"></script>
        {% endjavascripts %}
                
<script type="text/javascript">
$(function() {
  var banner = $('.banner');
  var backgrounds = ["{{asset('bundles/jbnetbootstraptheme/images/image1.png')}}",'url(https://placehold.it/1200x800)','url(https://placehold.it/1200x801)'];
  var current = 0; // Index image courrante // background[0] = images/image1.pn 0 => 2

function nextBackground() {
  // .css('parametre', 'valeur')
  banner.css(
   'background',
    backgrounds[current = ++current % backgrounds.length]+' no-repeat' // background : url(images/image1.png) no-repeat;
 );

 banner.css('background-size', 'cover');

 setTimeout(nextBackground, 5000);
 }

 
 setTimeout(nextBackground, 5000); // 5s 

   banner.css('background', backgrounds[0]+' no-repeat');
   banner.css('background-size', 'cover');
 });
</script>
<script type="text/javascript">
	$(function() {
		$('.banner-content h1').fadeIn(3000);
		$('.banner-content h2').fadeIn(4000);
	});
</script>  
  {% endblock %}
<a>Mentions l&eacute;gales</a>
<a>A propos</a>
<a>Plan du site</a>
<a href="#page">Haut de page</a>
</html>
